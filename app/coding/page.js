import { lusitana } from '@/app/ui/fonts';

export default function Page() {
    return <h1 className={`${lusitana.className} mb-3 text-2xl`}>
        Testing Page 입니다. 이렇게 예외 페이지가 됩니다.
        </h1>
}